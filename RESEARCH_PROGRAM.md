# DRREM — программа исследования, v0 (2026-09-18)

Документ ставит научную задачу по README.md и переводит её в проверяемые
гипотезы и лестницу проб на реальном тексте. README — источник идей; здесь
идеи доводятся до формы, которую можно опровергнуть измерением.

## 0. Рамки

Зафиксировано постановщиком (не обсуждается внутри проб):

1. Данные — только реальный текст. Обучение — только OpenOrca (равномерный
   срез 100 000 диалогов из GPT-4-части, seed 20260918) в режиме «потеря на
   ответе»: промпт читается свободной динамикой, подталкивание и все члены
   обучения действуют только на байтах ответа. GSM8K (main, train/test) в
   обучение не входит — это контроль вне распределения (OOD). Синтетических
   корпусов, игрушечных грамматик, случайных последовательностей нет нигде,
   включая отрицательные примеры для FF (см. §6).
2. Машина: двунаправленная, рекуррентная, полносвязная, многоуровневая;
   обучение — локальные правила семейства Forward-Forward и STDP, без
   backprop как метода обучения. Autograd допустим только как измерительный
   прибор (диагностика выравнивания правила с истинным градиентом, §3.7).
3. Цель — генерация текста и исследование сверхсходимого потенциала
   (скорость усвоения статистики за один проход) и нетривиальных принципов
   работы (итеративный вывод, re-entrant обработка, адаптивная глубина,
   спонтанная структурная активность). SOTA-паритет целью не является.
4. Два токенизатора как контролируемая переменная: байты через CNN по байтам
   и BPE Qwen2.5-1.5B.
5. Никаких прототипов «на выброс»: пакет строится сразу как рабочий стенд,
   пробы — его конфигурации, а не отдельные скрипты.

Железо: RTX 3080 Laptop 8 ГБ, 16 ядер, 30 ГБ RAM. Окружение —
`~/Coding/Python/CERBER/.venv` (torch 2.6 + cu124).

## 1. Аудит README: что зафиксировано и где дыры

README задаёт правильный скелет: разложение связей на симметричную память
$S$ и антисимметричный транспорт $A$; STDP-ядро, разложенное на чётную и
нечётную части; FF как локальный модулятор; якорение каждого уровня к
токенам через общую матрицу $E$; цену маршрута и адаптивную остановку;
re-entrant обработку; спонтанную активность. Но как спецификация обучения
он не замкнут. Дыры, которые пробы обязаны закрыть, а не обойти:

- **D1. Скалярный трёхфакторный сигнал.** README предлагает
  $\Delta w_{ij}=\eta\,m_\ell\,e_{ij}$ с наградой $R=\Delta\log p(x_{t+1})-\text{cost}$.
  Это REINFORCE по $N^2$ синапсам на уровень: дисперсия оценки градиента
  растёт с числом синапсов, и на реальном тексте такое правило не сойдётся
  за разумное число токенов. Нужен **векторный** локальный сигнал ошибки
  (по одному числу на нейрон, а не одно число на уровень).
- **D2. Мост «чёт/нечет STDP → S/A» дан как эвристика.** Проверено
  численно (`scripts/verify_parity_identities.py`): трассовое STDP с нечётным
  ядром антисимметрично по паре синапсов $(ij,ji)$ **точно** и потому пишет
  только в $A$; в $S$ оно не может записать ничего, и никакой модулятор это
  не исправит. Градиент для $S$ даёт не тайминг, а контраст двух фаз
  (EqProp/FF, §3.2). Разделение труда точнее, чем в README: фазы учат
  память, тайминг учит транспорт (§3.3).
- **D3. Равновесие против транспорта.** Энергетическая память требует
  сходимости к неподвижной точке; $A$ по определению её разрушает.
  README этот конфликт не разрешает. Мы его измеряем (§3.7) и разводим по
  масштабам времени (§2.3).
- **D4. Негативы для FF.** «Lyon is the capital of France» — рукотворная
  синтетика, запрещена. Негативы берутся только из самой машины
  (self-negatives) или из реального текста с чужим контекстом (§6).
- **D5. Цена маршрута и вентили на рёбрах** — экономический механизм,
  навешенный поверх RL. На $N\le 2048$ плотное $\mathbf W s$ на 3080 стоит
  единицы миллисекунд; вычислительная разреженность приходит бесплатно от
  разреженности событий $s$. Когнитивную часть — «стоит ли ещё один хоп» —
  можно обучать с учителем по реализованному выигрышу (§7), без RL. Вентили
  $g_{ij}$ откладываются до того, как машина вообще научится учиться.
- **D6. Спайки или аналог** не решено; синапсы с четырьмя шкалами
  умножают параметры на 4. Решение: градуированные разреженные события и
  шкалы на уровне нейрона, не синапса (§2.1, §4).
- **D7. Межтактовое присвоение заслуг отсутствует.** STDP-eligibility в
  README живёт внутри хопа. Контекст должен переносить не только состояние,
  но и след «кто участвовал» (§3.4).
- **D8. Устойчивость** сведена к списку «leak + refractory + inhibition» без
  измеримого критерия. Вводится контроль ветвления каскадов и спектрального
  радиуса (§4, задачи-измерения).

## 2. Формальная машина

### 2.1. Состояние и события

Уровни $\ell=1..L$, по $N$ нейронов. Нейрон $i$ уровня $\ell$ имеет:

- $x$ — мембранное состояние (аналог, ℝ);
- $s$ — событие: $s=\varphi(x-\theta)\ge 0$, разреженное (ноль ниже порога);
- $a$ — адаптация; $\theta=\theta_0+\beta_a a$ — адаптивный порог;
- $x^{(m)}$, $m=1..4$ — следы событий с $\tau=(2,8,32,128)$ тактов;
- $c_{m}$ — собственный временной профиль нейрона: чем он «слышен» другим,
  $\tilde s=\sum_m c_m x^{(m)}$ (4 числа на нейрон, не на синапс).

$\varphi$ — переменная плоскость поиска (§4): градуированный порог
(жёсткая сигмоида $\min(1,\max(0,x-\theta))$ либо $\max(0,x-\theta)$),
бинарный спайк с рефрактерностью. Стохастическая каноническая машина
Больцмана исключена решением постановщика (2026-09-18).

### 2.1а. Явная функция нейрона (реализация v3, 2026-09-18)

Нейрон $i$ — не $\sigma(w^\top x)$, а объект с быстрым состоянием и тремя
медленными переменными, каждая со своим обучаемым свойством:

$$
x_i\leftarrow(1-\alpha)x_i+\alpha\Big[\sum_j W_{ij}\big(s_j+\sum_m c_{jm}\,\mathrm{trace}_{jm}\big)+I_i-g_i a_i+\mathrm{DAM}_i+\mathrm{force}_i\Big],\quad s_i=\rho(x_i-\theta_i),
$$

раз в такт своего уровня: $\mathrm{trace}_{im}\leftarrow\lambda_m\mathrm{trace}_{im}+(1-\lambda_m)s_i$
($\tau_m=2,8,32,128$), $a_i\leftarrow\lambda_a a_i+(1-\lambda_a)s_i$,
$\theta_i\leftarrow\theta_i+\eta_\theta(\langle s_i\rangle-r^*)$. Обучаемые свойства нейрона:
$c_{im}$ — на каких шкалах его слышат другие («когда активироваться, учитывая
что было и когда» из README), $g_i$ — как быстро он устаёт, $\theta_i$ —
гомеостатически. Правила локальны и выведены из энергии:
$\Delta c_{im}\propto\mathrm{trace}_{im}(W^\top d)_i/\beta$,
$\Delta g_i\propto-d_i a_i/\beta$, $d=s^\beta-s^{tw}$; проверены autograd
(косинус > 0,99, наклон 1; `tests/test_machine2.py`, `reports/P1.md`).
Фаза $\phi_i$ из README (осцилляторное кодирование) пока не реализована.

### 2.2. Связность

Полный вектор событий $\mathbf s\in\mathbb R^{LN}$. Матрица
$\mathbf W\in\mathbb R^{LN\times LN}$ блочно-трёхдиагональная (радиальная
структура: уровень связан с собой и соседями), плотная внутри блоков:

$$
\mathbf W=\mathbf S+\mathbf A,\qquad \mathbf S=\mathbf S^\top,\ \mathbf A=-\mathbf A^\top .
$$

Двунаправленность — это симметрия $\mathbf S$: блок $\ell\to\ell+1$ и блок
$\ell+1\to\ell$ суть транспонированные копии, сигнал ходит вверх и вниз
одними и теми же ассоциациями. Направленность — это $\mathbf A$.

Токены входят через $E\in\mathbb R^{V\times N}$ в уровень 1 (BPE) либо через
CNN-фронт по байтам (§5). Каждый уровень читается той же $E$:
$p_\ell=\operatorname{softmax}(E s_\ell/\tau_r)$.

Параметры при $N=1024$, $L=4$: блоки $4+3=7$, по два ($S,A$) — около 14 М
чисел плюс $E$. При $N=2048$ — около 59 М. Обе конфигурации помещаются в
8 ГБ с батчем и следами.

### 2.3. Энергия и динамика хопа

Симметричная часть задаёт энергию непрерывной сети Хопфилда

$$
\mathcal E(\mathbf x)=\sum_i \Phi(x_i)-\tfrac12\,\mathbf s^\top\mathbf S\mathbf s-\mathbf s^\top\mathbf I,
\qquad \Phi'=\varphi^{-1},
$$

а динамика одного хопа $k$ внутри такта токена $t$:

$$
\mathbf x_{k+1}=(1-\alpha)\mathbf x_k+\alpha\big[(\mathbf S+\mathbf A)\tilde{\mathbf s}_k+\mathbf I_t+\boldsymbol\xi_k-\mathbf a_k\big],
\qquad
\mathbf s_{k+1}=\varphi(\mathbf x_{k+1}-\boldsymbol\theta_k).
$$

При $\mathbf A=0$ это градиентный спуск по $\mathcal E$ — сходится к
аттрактору. При $\mathbf A\ne0$ — векторное поле с вращением: траектории,
циклы, переходы. Разведение по масштабам времени, которое снимает D3 в
первом приближении: внутри такта $\mathbf A$ действует с малым весом
$\gamma_{in}$ (машина «думает» преимущественно релаксацией), между тактами —
с полным (машина «движется» по последовательности). $\gamma_{in}$ — ось
поиска; $\gamma_{in}=1$ проверяется отдельно.

### 2.4. Такт токена

На такте $t$: подаётся $\mathbf I_t=E[x_t]$ (или выход CNN), делается $H_t$
хопов, после каждого хопа доступно чтение $p_{\ell,k}$ на каждом уровне.
Состояние $\mathbf x$, следы и адаптация переносятся на такт $t+1$ —
это и есть контекст. Число хопов $H_t$ либо фиксировано, либо выбирается
правилом остановки (§7).

### 2.5. Генерация — это свободный ход

После промпта внешний вход убирается; на каждом такте чтение с уровня
$\ell^*$ даёт распределение, из него берётся токен (жадно или при
температуре) и подаётся как $\mathbf I_{t+1}$. Второй режим — мягкая
обратная связь $\mathbf I_{t+1}=E^\top p$ без выбора токена (непрерывная
мысль без фиксации слова); обе плоскости измеряются.

## 3. Единое правило обучения: две фазы × STDP-форма × чётность матрицы

Это центральное предложение документа. README хотел «FF модулирует STDP»;
здесь FF и STDP оказываются двумя проекциями одного правила.

### 3.1. Две фазы (FF)

Forward-Forward работает на двух прогонах: положительном (данные) и
отрицательном. У нас:

- **свободная фаза** — хопы при входе $\mathbf I_t$ без знания ответа;
  конечное состояние $\mathbf s^0$;
- **подталкиваемая фаза** — те же хопы, но к каждому уровню с чтением
  добавляется сила
  $$\boldsymbol\nu_\ell=\beta\,E^\top\big(y_t-p_\ell\big),$$
  где $y_t$ — one-hot истинного следующего токена. Это $-\partial C/\partial s_\ell$
  кросс-энтропии — локальное дельта-правило чтения, один матмул на уровень;
  конечное состояние $\mathbf s^\beta$.

Положительная фаза FF = подталкиваемая, отрицательная = свободная. Третья,
FF-специфичная, фаза — подталкивание к собственному предсказанию машины
$\hat y\sim p_\ell$ (self-negative), даёт состояние $\mathbf s^{\hat\beta}$;
её роль — контрольная переменная (гипотеза H3).

### 3.2. STDP-форма, контраст фаз и что кому доступно

Введём приращение пост-активности за хоп $\Delta s_j(t)=s_j(t)-s_j(t-1)$ и
накопим по переходному процессу подталкиваемой фазы (от $\mathbf s^0$ к
$\mathbf s^\beta$) величину

$$
T_{ij}=\sum_t s_i(t)\,\Delta s_j(t)
$$

— «пре × приращение пост», STDP-совместимая форма Bengio et al. 2017.
Точное дискретное тождество (проверено до $10^{-15}$ на искривлённой
траектории, `scripts/verify_parity_identities.py`):

$$
T_{ij}+T_{ji}=\big(s_i^\beta s_j^\beta-s_i^0s_j^0\big)+\sum_t\Delta s_i\,\Delta s_j .
$$

Первое слагаемое — контраст Equilibrium Propagation, равный
$\beta\,\partial C/\partial S_{ij}$ при малом $\beta$ и достигнутых
равновесиях (Scellier & Bengio 2017). Второе — поправка второго порядка:
15 % от нормы контраста при 8 хопах, 3,8 % при 32, 0,9 % при 128. Поэтому
для $S$ используется не накопление по хопам, а сам контраст концов
$\mathbf s^\beta\mathbf s^{\beta\top}-\mathbf s^0\mathbf s^{0\top}$:
два внешних произведения, локально и без поправки.

Отрицательный факт, важнее положительного. Стандартное STDP по следам —
«пост-событие × след пре минус пре-событие × след пост» с нечётным ядром —
даёт матрицу, антисимметричную **точно**, для любой траектории и любого
окна ($|\Delta W+\Delta W^\top|_{\max}=0$ численно, при любой длине
перехода и любой $\tau$). Оно не может изменить симметричную часть связи
ни на что: контраст EqProp ему недоступен принципиально, и никакой
модулятор — goodness, награда — этого не меняет. README-схема
«STDP × FF-модулятор» в чистом виде оставила бы память $S$ необученной.

### 3.3. Чётность: фазы учат $S$, тайминг учит $A$

$$
\Delta S=\tfrac12\big(\mathbf s^\beta\mathbf s^{\beta\top}-\mathbf s^0\mathbf s^{0\top}\big)
\quad\text{(контраст фаз, EqProp)},
$$

$$
\Delta A=\tfrac12\sum_t\big(\mathbf s\,\Delta\mathbf s^\top-\Delta\mathbf s\,\mathbf s^\top\big)
\quad\text{(циркуляция)} .
$$

Циркуляция — ровно то, что вычисляет трассовое STDP с нечётным ядром
(косинус 0,89 при 8 хопах, 0,96 при 32, 1,000 при 128 и более), так что
$A$ учится обычным STDP по следам без оговорок. Для прямолинейного
перехода с коррекцией $\mathbf d=\mathbf s^\beta-\mathbf s^0$ циркуляция
равна клину $\tfrac12(\mathbf d\,\mathbf s^{0\top}-\mathbf s^0\mathbf d^\top)$,
и новое поле в свободной точке

$$
\Delta A\,\mathbf s^0=\tfrac12\|\mathbf s^0\|^2\,\mathbf d_\perp
$$

(косинус 1,0, масштаб 1,000 численно) направлено вдоль компоненты
коррекции, ортогональной текущему состоянию. Антисимметричная часть не
меняет энергию — она учит направленное движение из состояния, где машина
была, в состояние, где ей следовало быть. На уровне матрицы это аналог
разложения Гельмгольца: потенциальная часть коррекции — в память,
вихревая — в транспорт.

Итого: **FF даёт фазы и учит память $S$; STDP даёт тайминг и учит
транспорт $A$.** Одно событие — переход к правильному ответу — читается
двумя правилами, каждое из которых по построению пишет только в свою
половину $\mathbf W$.

### 3.4. Межтактовые члены

Чётное ядро (корреляция без сдвига) в свободной фазе — авто-ассоциация,
классическое хранение состояния в $S$; его антисимметричная часть нулевая.
Чётное ядро **со сдвигом на такт** $\int s_i(t)s_j(t+1)$ имеет ненулевую
антисимметричную часть — память порядка (Sompolinsky & Kanter 1986):
«после $A$ обычно $B$». Она идёт в $A$.

Присвоение заслуг через такты (D7): следы годности
$e_{ij}(t)=\rho\,e_{ij}(t-1)+s_i(t)\,\tilde s_j(t)$ и локальный сигнал
$\boldsymbol\nu_\ell(t)$ на текущем такте дают e-prop-подобный член
$\Delta W_{ji}\propto \nu_j(t)\,e_{ij}(t)$ (Bellec et al. 2020). Память —
один след на синапс, как сам вес; при $N=1024$ это те же 14 М чисел.
Включается как отдельная ось (проба P5).

Итого четыре локальных члена с ясными ролями:

| Член | Ядро | Фаза | Адресат | Роль |
|---|---|---|---|---|
| контраст | концы близнецовых фаз, внешние произведения | подталк. − близнец | $S$ | градиент чтения |
| клин | концы близнецовых фаз | подталк. − близнец | $A$ | транспорт коррекции (P0: точен без сходимости) |
| циркуляция | нечётное STDP по следам, внутри такта | подталк. − близнец | $A$ | физическая форма клина; точна только при продолжении из конца |
| порядок | чётное со сдвигом на такт | свободная | $A$ | что за чем следует |
| авто-ассоциация | чётное без сдвига | свободная | $S$ | хранение состояния (ось: вкл/выкл) |
| годность | след × $\nu$ | обе | $S,A$ | заслуги через такты |

Какое подмножество реально учит текст — это и есть первая серия проб.

### 3.5. Чтение, пороги, гомеостаз

$E$ учится дельта-правилом $\Delta E_v\propto(y_v-p_v)\,\mathbf s_\ell$
(локально, без переноса через уровни). Пороги — гомеостатически к целевой
частоте событий $r^*$; нормы входящих весов — правилом Ойи (иначе Хебб в
полносвязной сети разгоняется). Обе величины — не гиперпараметры «на глаз»,
а измеряемые регуляторы: за $r^*$ и нормой следят метрики каждой пробы.

### 3.5а. Что значит замер активаций FF в нашей машине

Goodness Хинтона $\sum_i h_i^2$ есть диагональ энергии Хопфилда $-\tfrac12 s^\top Ss$, а
его правило по слою — контраст фаз, ограниченный самосвязями:
$\Delta S_{ii}\propto(s_i^\beta)^2-(s_i^0)^2$. Машина учит весь квадратичный
ландшафт, FF — его диагональ. Поэтому «замер активаций на положительных и
отрицательных данных» у нас имеет три прямых применения: (i) смещение
$d=s^\beta-s^{tw}$ — удивление в пространстве состояний: сигнал уверенности,
остановки и по-нейронный нейромодулятор; (ii) энергия свободного состояния —
оценка «знакомости» входа без цели; (iii) $d$ уровня 1 — тот вход, которого
машине не хватило: $-\partial C/\partial I\approx d/\beta$, и проекционный слой
фронта учится его давать дельта-правилом, а свёрточные слои — FF-goodness на
реальных окнах против окон с хвостом из собственных предсказаний машины.

### 3.6. Что здесь FF, что STDP, что новое

- FF: структура фаз, self-negatives, при желании — цель «goodness»
  $\sum s^2$ на уровень как добавочный модулятор (ось поиска; ожидание —
  избыточен при векторном $\nu_\ell$, что нужно показать, а не предположить).
- STDP: трассовое правило с нечётным ядром для $A$, чётные корреляции для
  авто-ассоциации и порядка; реализация через следы и внешние произведения.
- Новое: точная граница возможностей STDP (§3.2) как основание разложения
  S/A; циркуляция как учитель транспорта; готовность мерить, где теория
  ломается.

### 3.7а. Результат P0: близнецовые фазы (2026-09-18)

Проба P0 (`reports/P0.md`) измерила правило на реальных байтах против
autograd-градиента. Где свободная фаза сходится, контраст и клин совпадают
с градиентом (косинус 0,93–0,999, наклон ≈ 1), но сошедшаяся машина с
одним быстрым состоянием **не имеет памяти**: неподвижная точка единственна
и зависит только от текущего байта. Память есть лишь в несошедшемся
режиме, где последовательные фазы (подталкивание из конца свободной)
ломаются: контраст измеряет продолжающуюся релаксацию, а не отклик на
подталкивание (косинус −0,03 при 4 хопах).

Решение, проверенное измерением: **близнецовые фазы** — отрицательная фаза
стартует из того же состояния такта, что и подталкиваемая, идёт столько же
хопов; все правила берут разность близнецов
($\Delta S\propto s^\beta s^{\beta\top}-s^{tw}s^{tw\top}$, клин по концам).
Косинус при 4 хопах 0,97 при сохранённой памяти, при 2 хопах 0,875. Это
FF-структура фаз в чистом виде (положительная и отрицательная из одного
старта), и она снимает D3 на уровне правила: сходимость не нужна.
Траекторные правила для $A$ (циркуляция, трассовое STDP) точны только в
режиме продолжения из конца свободной фазы; в машине $A$ учится клином.

В замкнутом цикле (обучение по ответу, 300 батчей, 4,3 М байт) правило
довело машину $N=256$ с 8,2 до 3,72 бит/байт на отложенных документах
(биграмма 3,664) при близнецовых фазах и распаде весов $10^{-4}$; память
выросла с 0,11 до 0,6. Без контроля нормы $\|A\|$ растёт линейно, 40–70 %
единиц насыщаются, потеря ухудшается после 50 батчей; сошедшийся режим
насыщается при любом шаге и застревает на 4,1–4,3. Следствие для §3.5:
гомеостаз порогов и масштабирование норм — условие обучаемости, а не
гигиена; они входят в P1 первыми.

### 3.7. Где теория ломается и что мерить

1. **Равновесие.** EqProp точен при достигнутых неподвижных точках.
   Меряем $\|\dot{\mathbf s}\|$ в конце каждой фазы и долю тактов без
   сходимости.
2. **Конечное $\beta$.** Смещение оценки $O(\beta)$; двусторонний вариант
   $\pm\beta$ (Laborieux et al.) снимает первый порядок. Ось поиска.
3. **$\mathbf A\ne0$.** Якобиан несимметричен, контраст перестаёт быть
   градиентом (Scellier et al. 2018, векторные поля). Единственный честный
   ответ — измерить: косинус между локальным обновлением $S$ и истинным
   градиентом кросс-энтропии через $H$ хопов (autograd как прибор) на
   реальных батчах, при $\gamma_{in}\in\{0,0.25,1\}$. Это проба P0, и она
   идёт раньше любого обучения: если косинус около нуля, правило не учит,
   а коррелирует, и неделя обучения ничего не покажет.

## 4. Плоскости поиска

**Функции.**

| Ось | Варианты | Стартовое |
|---|---|---|
| $\varphi$ — событие | градуированный порог (жёсткая сигмоида / ReLU); бинарный спайк + рефрактерность | жёсткая сигмоида |
| STDP-ядро | экспоненциальная пара ($\tau_\pm$); сумма шкал; параметры ядра на уровень | пара, $\tau_\pm$ фикс. |
| Разложение по чётности | вкл. / выкл. (единая $W$) | вкл. |
| Негативная фаза | свободная; self-negative; чужой реальный контекст | свободная, затем self |
| Goodness FF | нет; $\sum s^2$; logsumexp-зазор | нет |
| Остановка | фикс. $H$; хоп-дропаут (случайное $H\in\{2,4,8,16\}$ при обучении, читаемость на любом хопе); голова ценности; порог энтропии чтения | хоп-дропаут, затем голова |
| Обратная связь при генерации | токен; $E^\top p$ | токен |
| $\gamma_{in}$ | 0; 0.25; 1 | 0.25 |

**Переменные.** Нейрон: $x,s,a,\theta,x^{(1..4)},c_{1..4}$. Синапс: $S,A$,
след годности $e$. Задержки $d_{ij}$ и вентили $g_{ij}$ — отложены (D5).
Глобальные: $\beta$, $T$, $H_{\max}$, $r^*$, $\alpha$, $\eta_S,\eta_A,\eta_E$.

**Задачи-измерения (всё на реальном тексте, без исключений).**

| Измерение | Что показывает |
|---|---|
| CE на отложенной выборке, бит/байт и нат/токен | учится ли вообще |
| $\ell(n)$ — CE после $n$ увиденных токенов, один проход | сверхсходимость |
| Одноразовое праймирование: потеря при повторе документа | быстрая память |
| CE по хопам $p_{\ell,1}\dots p_{\ell,H}$ | итеративный вывод |
| Абляция сверху вниз (маска $\mathbf S^\top$ между уровнями на выводе) | re-entry |
| Корреляция выбранного $H_t$ с условной энтропией | адаптивная глубина |
| Косинус с истинным градиентом (P0) | правило учит или коррелирует |
| Коэффициент ветвления каскадов $\sigma$ (Wilting & Priesemann) | критичность |
| Спектральный радиус якобиана в аттракторе | устойчивость |
| Кластеры совместной активности при слове/понятии | ансамбли |
| Свободный ход после разреженного семени | спонтанная структура |
| Образцы генерации, жадно и при $T$ | текст |

## 5. Токенизаторы

**Байты через CNN.** Вход — окно последних $K$ байт (one-hot 256), 3–4
свёрточных слоя с дилатациями, выход $u\in\mathbb R^N$ на уровень 1.
Фронт обучается **тоже локально**, по FF: положительные окна — реальные,
отрицательные — окна, хвост которых заменён байтами, сгенерированными самой
машиной (self-negative), а до того, как машина что-то порождает, — хвостом
из другого реального документа (реальный текст, чужой контекст). Чтение —
256-полосный softmax через $E_b\in\mathbb R^{256\times N}$ (дельта-правило).
Довод «за»: обратимость и отсутствие произвольных границ; и именно здесь
адаптивная глубина видна на просвет — большинство байт внутри слова дёшевы,
дорогие байты (начало слова, цифры в GSM8K) должны получать больше хопов.
Довод «против»: в 4 раза больше тактов на текст; чтение $p$ на каждом хопе
дёшево (256), это плюс.

Контроль B0 (только абляция): байты как события напрямую в уровень 1, без
CNN — показывает, сколько именно даёт фронт. Реализация v3
(`frontend/bytes_cnn.py`): свёрточные слои — FF по слоям с нормировкой между
слоями, проекция — дельта-правило на смещении машины (§3.5а).

**Qwen2.5-1.5B.** Словарь 151 665; в обучении участвуют только встреченные
токены — 78 952 в срезе. $E$ общая для входа и чтения. Ограничение,
которое нужно знать заранее: одно чтение $78\,952\times1024\approx81$ М МАС
против $\approx7$ М МАС на весь хоп динамики ($N=1024$, $L=4$); по-хоповое
чтение на четырёх уровнях дороже динамики примерно в 45 раз. На этой
плоскости чтение при обучении делается в конце фазы и сэмплированным
softmax (несколько тысяч негативов), полное — только при оценке на
отложенной выборке; по-хоповое чтение для остановки — только с байтами.

Оба токенизатора идут по одной лестнице проб; байты с CNN-фронтом идут
первыми (решение постановщика), Qwen — после P1 на той же конфигурации.

## 6. Данные и отрицательные примеры

Срез фиксирован `scripts/fetch_data.py` и `data/manifest.json` (sha256).
Обучение — только OpenOrca: строка `system_prompt \n question \n response`,
потеря и подталкивание только на байтах ответа (промпт — контекст).
Отложенная выборка — 2 000 диалогов OpenOrca (seed-перестановка), в
обучение не попадают. GSM8K (`question \n answer`, маркер `####`
сохраняется) — только OOD-контроль: потеря на ответе без обучения.

Числа зафиксированного среза (`data/manifest.json`, 2026-09-18):

| Корпус | Док. | Байт | Токенов Qwen | Байт/токен | Медиана док., байт | $H_1$, бит/байт | $H_2$, бит/байт |
|---|---|---|---|---|---|---|---|
| OpenOrca-100k | 100 000 | 175,8 М | 38,57 М | 4,56 | 1 326 | 4,645 | 3,664 |
| GSM8K train | 7 473 | 3,91 М | 1,356 М | 2,88 | 485 | 4,917 | 3,503 |
| GSM8K test | 1 319 | 0,70 М | 0,245 М | 2,88 | 499 | 4,912 | 3,501 |

$H_1$ — энтропия байтовых униграмм, $H_2$ — условная энтропия биграмм;
это нулевые линейки для H1. Состав OpenOrca-100k по источникам: flan 50,3 %,
t0 33,3 %, niv 8,8 %, cot 7,6 % — срез равномерный, а не префикс файла.
Из 151 665 токенов словаря Qwen в срезе встречается 78 952.

Что считается синтетикой и запрещено: любой текст, порождённый генератором,
не являющимся самой машиной; любые перестановки слов, случайные токены,
шаблонные факты. Что допустимо: собственные предсказания машины как
негативы (модель учится на своих ошибках — суть FF) и реальный текст с
несоответствующим контекстом.

## 7. Хопы, остановка, цена

Не RL. На каждом хопе $k$ известен реализованный выигрыш
$G_k=\log p_k(x_{t+1})-\log p_{k-1}(x_{t+1})$ (после того как ответ известен).
Голова ценности $V(\mathbf s_k)$ — линейное чтение с уровня, обучается
регрессией на $G_{k+1}$ дельта-правилом. На выводе: продолжать, пока
$V(\mathbf s_k)>c$, где $c$ — цена хопа в тех же единицах (наты). Цена —
измеряемый регулятор: кривая «CE против среднего $H$» при разных $c$ и есть
главный график README («качество против числа внутренних событий»).

## 8. Гипотезы

- **H1 (быстрая система).** Одноуровневая машина с чисто хеббовскими
  $S$ (авто-ассоциация), $A$ (порядок) и дельта-$E$, один проход,
  $10^6$ байт — CE ниже биграммной байтовой линейки 3,664 бит/байт.
  Опровержение: не ниже при $10^7$ байт.
- **H2 (итеративный вывод).** С фазами (§3.1–3.3) CE по хопам убывает в
  среднем на отложенной выборке, и CE($H=8$) < CE($H=1$) значимо.
  Опровержение: выигрыш хопов $\le0$.
- **H3 (self-negative как контрольная переменная).** Контраст
  «подталк. к истине − подталк. к своему предсказанию» даёт более крутую
  $\ell(n)$, чем «подталк. − свободная». Опровержение: нет разницы или хуже.
- **H4 (re-entry).** Маска сверху вниз на выводе ухудшает CE при $L\ge2$.
  Опровержение: разница в пределах шума.
- **H5 (адаптивная глубина).** $H_t$ от головы ценности коррелирует с
  условной энтропией; цифры GSM8K получают больше хопов, чем буквы внутри
  слов; средний $H$ падает без роста CE. Опровержение: корреляция около нуля.
- **H6 (сверхсходимость).** $\ell(n)$ DRREM лежит ниже линейки равного
  числа параметров (обученной backprop) при $n\le n_0$; $n_0$ измеряется.
  Опровержение: $n_0<10^5$.
- **H7 (спонтанная структура).** После обучения разреженное семя (2–10
  нейронов) даёт каскады с $\sigma\approx1$, чьё чтение линейка оценивает
  как правдоподобный текст выше случайного уровня. Опровержение: каскады
  гаснут или синхронизируются.

## 9. Лестница проб

Каждая проба — конфигурация стенда, ограниченный бюджет GPU, критерий
прохода, артефакты (JSONL-метрики, кривые, образцы). Провал пробы —
результат, а не повод менять критерий.

| Проба | Конфигурация | Что меряем | Проход |
|---|---|---|---|
| P0 физика правила — **выполнена** (`reports/P0.md`) | $N=256$, $L\in\{1,2\}$, реальные батчи, 36 конфигураций | косинус локального $\Delta S$ с градиентом; контроли; память; тест снижения потерь | пройдена для близнецовых фаз (0,87–0,999); последовательные фазы проходят только при сходимости, где памяти нет |
| P1 быстрая система | $N=1024$, $L=1$, $H=1$, байты+CNN | $\ell(n)$, праймирование | ниже биграммы к $10^6$ байт |
| P2 фазы | + подталкивание, $H=4..8$ | CE по хопам, $\ell(n)$ | H2 |
| P3 уровни | $L=2\to4$, симметричные межуровневые блоки | абляция сверху вниз | H4 |
| P4 глубина | голова ценности, $c$ | $H_t$ против энтропии, CE против $\bar H$ | H5 |
| P5 такты и Qwen | e-prop-следы вкл/выкл; повтор P1–P3 на Qwen | $\ell(n)$, образцы | сравнение плоскостей |
| P6 спонтанность | семена, $T(\cdot)$, ансамбли | $\sigma$, кластеры, свободный ход | H7 |

Оценка стоимости (плотная динамика, $N=1024$, $L=4$, 2 фазы, $H=8$,
батч 64): 7 блоков $\times N^2\times16$ хопов $\times64\approx7{,}5$ ГМАС на
такт. Один проход по 38,6 М токенов Qwen — $4{,}5\cdot10^{15}$ МАС, около
15 минут чистой арифметики при 5 ТМАС/с, реально 1–2 часа с поэлементными
операциями и сэмплированным чтением. Байтовая плоскость: 176 М тактов,
$2\cdot10^{16}$ МАС — 3–5 часов при фиксированном $H=8$, 1–2 часа при
адаптивной остановке со средним $H\approx3$. P0 и P1 — минуты.
Все прогоны — ограниченные по времени, возобновляемые (чекпойнт состояния
и весов).

## 10. Линейки

Байтовые n-граммы на тех же документах и байтах ответа (`drrem/rulers/ngram.py`,
2026-09-18): 1-грамма 4,68, биграмма 3,67, триграмма 2,99, 4-грамма 2,44,
5-грамма 2,09 бит/байт на 33,6 МБ, увиденных машиной. Порог «выше n-грамм» —
5-грамма на тех же данных; лучшая машина на этот момент 3,65.

Не для гонки, а для измерения: биграммная и униграммная байтовые энтропии
(из манифеста) и одна маленькая сеть равного числа параметров, обученная
backprop, — ровно чтобы $\ell(n)$ было с чем сравнить в H6. Если линейка
не нужна, H6 переформулируется в абсолютных терминах $\ell(n)$.

## 11. Реализация

Пакет `drrem/`, без временных скриптов:

- `data/` — потоковые загрузчики зафиксированного среза, оба токенизатора,
  разбиение по документам, манифест;
- `core/state.py` — батчевое состояние машины; `core/dynamics.py` — хоп и
  такт; `core/plasticity.py` — следы, четыре члена §3.4, чётность, Ойя,
  гомеостаз; `core/readout.py` — чтение по уровням, дельта-$E$, голова
  ценности; `core/halting.py`;
- `frontend/bytes_cnn.py` — FF-обучаемый фронт по байтам;
- `probes/` — P0–P6 как конфигурации с критериями прохода;
- `diagnostics/` — косинус с градиентом, $\sigma$, спектральный радиус,
  ансамбли, свободный ход;
- `rulers/` — энтропии и малая backprop-сеть;
- `runs/` — конфиги, JSONL, чекпойнты; возобновляемый раннер.

Порядок работ: данные и токенизаторы → ядро динамики и пластичности →
P0 → P1 → дальше по лестнице. Каждый шаг с тестами на инвариантах
(симметрия $S$, антисимметрия $A$, тождество §3.2 численно, сохранение
частоты событий гомеостазом).

## 12. Решения постановщика (2026-09-18)

1. Стохастическая каноническая машина Больцмана — не делается.
2. GSM8K — не для обучения, только OOD-контроль. Обучение — OpenOrca,
   потеря только на ответе.
3. Байты и CNN-энкодер — первая плоскость токенизатора.
4. Линейка равного числа параметров на backprop — только измеритель для H6.
5. Отложенная выборка — 2 000 диалогов OpenOrca; GSM8K test — OOD.
6. P0 — реализовать полностью; результаты, особенно отрицательные,
   перепроверять и разбирать.

## 13. Литература

- Scellier, Bengio. Equilibrium Propagation. Front. Comput. Neurosci. 2017.
- Bengio, Mesnard, Fischer, Zhang, Wu. STDP-Compatible Approximation of
  Backpropagation in an Energy-Based Model. Neural Computation 2017.
- Scellier, Goyal, Binas, Mesnard, Bengio. Generalization of Equilibrium
  Propagation to Vector Field Dynamics. 2018.
- Ernoult, Grollier, Querlioz, Bengio, Scellier. Updates of Equilibrium Prop
  Match Gradients of Backprop Through Time in an RNN with Static Input.
  NeurIPS 2019; Equilibrium Propagation with Continual Weight Updates. 2020.
- Laborieux, Zenke. Holomorphic Equilibrium Propagation Computes Exact
  Gradients Through Finite Size Oscillations. NeurIPS 2022.
- Bellec et al. A solution to the learning dilemma for recurrent networks of
  spiking neurons (e-prop). Nat. Commun. 2020.
- Hinton. The Forward-Forward Algorithm. 2022.
- Sompolinsky, Kanter. Temporal Association in Asymmetric Neural Networks.
  PRL 1986; Kleinfeld. PNAS 1986.
- Xie, Seung. Equivalence of Backpropagation and Contrastive Hebbian
  Learning. Neural Computation 2003; Movellan. Contrastive Hebbian Learning
  in the Continuous Hopfield Model. 1991.
- Krotov, Hopfield. Dense Associative Memory. NeurIPS 2016; Ramsauer et al.
  Hopfield Networks is All You Need. ICLR 2021.
- Frémaux, Gerstner. Neuromodulated STDP and theory of three-factor learning
  rules. Front. Neural Circuits 2016.
- Zenke, Gerstner, Ganguli. The temporal paradox of Hebbian learning and
  homeostatic plasticity. Curr. Opin. Neurobiol. 2017; Oja 1982.
- Wilting, Priesemann. Inferring collective dynamical states from widely
  unobserved systems. Nat. Commun. 2018; Beggs, Plenz. J. Neurosci. 2003.
- Graves. Adaptive Computation Time. 2016; Hao et al. Coconut. 2024.
- Yu et al. MegaByte. NeurIPS 2023; Pagnoni et al. Byte Latent Transformer.
  2024.
- Millidge, Tschantz, Buckley. Predictive Coding Approximates Backprop Along
  Arbitrary Computation Graphs. Neural Computation 2022 — соседняя локальная
  схема, запасная плоскость, если контраст фаз не выравнивается с градиентом.
